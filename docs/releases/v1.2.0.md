# ColumnPad v1.2.0

Release date: 2026-03-11

## Highlights
- Professional repo structure with `src/` and `tests/` roots to support long-term scaling.
- Workflow Builder upgraded with built-in templates and JSON import/export.
- Lined-paper writing mode added (toolbar + View menu toggle).
- Workflow schema extended with category/version and new step types.

## Fixes
- Fixed layout JSON parsing for new language and lined-paper fields.
- Fixed Workflow Builder JSON dialogs by adding required file-dialog namespace wiring.

## Validation
- `dotnet clean .\\ColumnPadStudio.sln`
- `dotnet build .\\ColumnPadStudio.sln -c Release` (0 errors)
- `dotnet run --project .\\tests\\ColumnPadStudio.SmokeTests\\ColumnPadStudio.SmokeTests.csproj -c Release` (91 checks passed)
- `dotnet publish .\\src\\ColumnPadStudio\\ColumnPadStudio.csproj -p:PublishProfile=FolderProfile`

## Release Asset
- File: `src/ColumnPadStudio/publish/ColumnPadStudio.exe`
- SHA256: $hash
