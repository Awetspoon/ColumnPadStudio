# ColumnPad v2.2.3

Release date: 2026-06-13

## Updates
- Routed workflow JSON opened from the main File menu into the existing Workflow Builder import path.
- Cleaned workflow normalization so that section only performs useful cleanup work.
- Kept the existing save/load/export paths intact for `.txt`, `.md`, layout JSON, workspace-session JSON, and workflow exports.
- Rechecked the repaired user workspace files against the app loader.
- Rebuilt and launch-checked the single-file Windows EXE.

## Verification
- Release build passes.
- Domain tests pass.
- Smoke tests pass.
- User workspace-session JSON files validate through the app loader.
- Single-file Windows EXE publish was created and launch-checked.

## Release Artifact
- `ColumnPadStudio-v2.2.3-win-x64.exe`

## Known Notes
- The EXE is not code-signed, so Windows SmartScreen may warn on first launch.
