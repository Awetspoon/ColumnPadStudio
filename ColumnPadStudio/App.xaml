<Application x:Class="ColumnPadStudio.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource EditorSelectionBrush}"/>
            <Setter Property="SelectionTextBrush" Value="{DynamicResource EditorSelectionTextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource EditorForegroundBrush}"/>
            <Setter Property="SelectionOpacity" Value="0.65"/>
            <Setter Property="IsInactiveSelectionHighlightEnabled" Value="True"/>
        </Style>
    </Application.Resources>
</Application>
