<Window x:Class="ColumnPadStudio.Controls.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Border Padding="14">
        <StackPanel Width="420">
            <TextBlock Text="{Binding Message}" Margin="0,0,0,10" TextWrapping="Wrap"/>
            <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="OK" Width="90" IsDefault="True" Click="Ok_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
