<Window x:Class="ColumnPadStudio.App.Views.WorkspaceRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Workspace"
        Width="380"
        Height="184"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Workspace name" Margin="0,0,0,8" />
        <TextBox x:Name="WorkspaceNameTextBox"
                 Grid.Row="1"
                 MinWidth="260"
                 Text="{Binding WorkspaceName, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="84" Margin="0,0,8,0" Content="Cancel" Click="OnCancelClick" />
            <Button Width="84" Content="Rename" Click="OnRenameClick" />
        </StackPanel>
    </Grid>
</Window>
